apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  # Instalação oficial do ArgoCD Image Updater
  - https://raw.githubusercontent.com/argoproj-labs/argocd-image-updater/v0.12.2/manifests/install.yaml

# Patch para configurar o nível de log ou outras propriedades do Updater, se necessário.
