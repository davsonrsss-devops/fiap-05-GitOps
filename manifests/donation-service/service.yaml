apiVersion: v1
kind: Service
metadata:
  name: donation-service
  labels:
    app: donation-service
spec:
  type: ClusterIP
  selector:
    app: donation-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8082
