apiVersion: v1
kind: Service
metadata:
  name: volunteer-service
spec:
  type: ClusterIP
  selector:
    app: volunteer-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8083
